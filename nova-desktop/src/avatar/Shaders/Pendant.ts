export const pendantShaderRoadmap = {
  baseColor: "#38dfff",
  emission: true,
  pulseByState: true,
  notes: "Emission intensity is driven by state machine and thinking/talking events.",
} as const;
